{
  "name": "software-testing-assignment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "mocha",
    "coverage": "nyc --reporter=lcov --reporter=text mocha && nyc report --reporter=text-lcov | coveralls"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "chai": "^5.1.2",
    "coveralls": "^3.1.1",
    "mocha": "^10.7.3",
    "nyc": "^17.1.0"
  },
  "nyc": {
    "include": ["src/**/*.js"], 
    "exclude": ["test/**/*.js","src/.internal/*.js"], 
    "reporter": ["text", "lcov"],
    "all": true
  }
}
